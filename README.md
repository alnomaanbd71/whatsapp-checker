# WhatsApp Number Checker

A web-based tool to check WhatsApp number registration status.

## Features
- CSV file upload
- WhatsApp number validation
- Excel report download
- Web interface

## Deployment
1. Clone repository
2. Create `templates` directory with `index.html`
3. Build Docker image:
```bash
docker build -t whatsapp-checker .
