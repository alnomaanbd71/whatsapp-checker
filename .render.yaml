# render.yaml কনফিগারেশন (যদি ব্যবহার করেন)
services:
  - type: web
    name: my-chrome-app
    env: docker
    buildCommand: docker build -t my-chrome-app .
    startCommand: docker run -it --rm my-chrome-app
    envVars:
      - key: DEBIAN_FRONTEND
        value: noninteractive
