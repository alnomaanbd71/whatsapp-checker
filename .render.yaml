services:
  - type: web
    name: whatsapp-checker
    env: docker
    dockerfile: Dockerfile
    branch: main
    plan: standard
    envVars:
      - key: DISPLAY
        value: ":99"
    autoDeploy: yes
    healthCheckPath: /
    resources:
      memory: 2GB
      disk: 1GB
