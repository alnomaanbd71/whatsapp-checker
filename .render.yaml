services:
  - type: web
    name: whatsapp-web
    env: container
    plan: free
    repo: https://github.com/<your-username>/whatsapp-checker
    branch: main
    buildCommand: "docker build -t whatsapp-web ."
    startCommand: "docker run -p 10000:$PORT whatsapp-web"
